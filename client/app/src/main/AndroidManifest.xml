<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bookbook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookBook"
        android:usesCleartextTraffic="true">
        <activity android:name=".ChooseSeat"></activity>
        <activity android:name=".BookTicket" />
        <activity android:name=".ReportBug" />
        <activity android:name=".ChangePW_Change" />
        <activity android:name=".ChangePW_Forget" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".AccountSetting" />
        <activity android:name=".Cart" />
        <activity android:name=".InformationMovie" />
        <activity android:name=".Searching" />
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUp" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>